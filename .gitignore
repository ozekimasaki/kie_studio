# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Electrobun build outputs
build/
artifacts/
release/
# Inno Setup staging (extracted app tree; generated by build-win-installer.mjs)
installer/win/staging/
installer/win/_probe/
# Bun lockfile (依存管理は npm。bun.lock は WSL ビルドの副産物)
bun.lock
*.local
.env
.env.local

# Local studio SQLite
data/
*.db
*.db-shm
*.db-wal
# Local at-rest secret key (sibling of studio.db; never commit)
*.key

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local agent / skill tooling (not part of the app)
.agents/
.claude/
.cursor/
.kiro/
skills-lock.json

# indexion: keep wiki, ignore generated local cache
.indexion/cache/
.indexion/digest/
